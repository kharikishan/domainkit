# Contract for {{name}}
# This file defines the formal interface of the {{name}} domain

models:
  - name: {{titleCase name}}
    fields:
      - name: id
        type: string
        required: true
      # Add more fields...

# api:
#   routes:
#     - method: GET
#       path: /api/{{name}}
#       description: List all {{name}} items

# events:
#   - name: {{name}}.created
#     description: Fired when a new {{name}} is created

dependencies: []
